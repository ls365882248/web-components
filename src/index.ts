import { AppLogin, AppButton, UserCard } from './packages'

window.customElements.define('my-button', AppButton);
window.customElements.define('user-card', UserCard);
window.customElements.define('app-login', AppLogin);